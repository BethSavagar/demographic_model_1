# transmission parameters script

if(transmission == T){
  beta <- NA # transmission rate, S-E (per hour)
  sigma <- NA # rate of becoming infectious , E-I (per hour)
  gamma <- NA # rate of becoming recovery, I-R (per hour)
  pR <- 0.5
}